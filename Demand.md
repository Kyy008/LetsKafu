# 页面实现需求整理

本文档仅整理和前端页面实现代码直接相关的需求，综合自 `ReadME.md`、`Report.md` 以及老师补充的功能说明。

> 说明：老师补充中提到的基础功能和附加功能，本整理均按需要实现处理。

## 1. 实现范围

- 作业类型为移动端商城静态页面。
- 页面为静态展示页，不要求动态交互、接口请求或复杂脚本逻辑。
- 主要实现文件为 `src/index.html` 和 `src/style.css`。

## 2. 代码结构要求

- 页面结构写在 `src/index.html`。
- 所有样式统一写在 `src/style.css`。
- 不使用 `<style>` 标签，不使用内联 `style`。
- 页面用到的图片资源统一放在 `src/assets/`。
- 建议使用清晰、语义化的结构和类名，保持代码整洁、可读。

## 3. 页面整体结构

页面应至少包含以下三大区域：

- `header`
  - 顶部搜索框
  - 商品分类区域
- `content`
  - 广告横图 / Banner
  - 商品列表
- `footer`
  - 底部导航栏

## 4. 必做页面模块

### 4.1 搜索框

- 页面顶部需要有搜索框。
- 搜索框应体现移动端商城首页常见样式，可包含搜索图标和输入框。

### 4.2 商品分类

- 页面中需要有商品分类入口区域。
- 每个分类项建议包含图标和文字。
- 分类区域适合横向排列，可做成一排入口或可横向滚动区域。

### 4.3 广告横图 / Banner

- 页面中需要有广告横图区域。
- 至少展示 1 张促销图。
- Banner 视觉上应占据内容区较醒目的位置。

### 4.4 商品列表

- 页面中需要有商品列表区域。
- 每个商品卡片至少包含：
  - 商品图片
  - 商品名称
  - 商品价格
- 商品列表建议使用双列或适合移动端的卡片式布局。

### 4.5 底部导航栏

- 页面底部需要有导航栏。
- 导航项至少体现类似“首页、分类、购物车、我的”等入口。
- 底部导航栏应固定在页面底部展示。

### 4.6 悬浮组件

- 页面中必须有 1 个悬浮组件。
- 可选形式包括：
  - 悬浮客服按钮
  - 返回顶部按钮
  - 悬浮广告按钮
- 该组件应使用固定定位，始终悬浮在页面右下角或合适位置。

## 5. CSS 与布局技术要求

### 5.1 Flex 布局

- 页面中必须使用 Flex 布局。
- 至少使用 5 个 Flex 相关属性。
- 可从以下属性中选择并落实到实际布局：
  - `display: flex`
  - `flex-direction`
  - `justify-content`
  - `align-items`
  - `flex-wrap`
  - `gap`

### 5.2 伪类 / 伪元素

- 页面中至少使用 2 种伪类或伪元素。
- 可选示例：
  - `:hover`
  - `::before`
  - `::after`
  - `:nth-child(...)`

### 5.3 CSS 动画

- 需要使用至少 1 处 CSS 动画。
- 可用于 Banner、按钮、卡片出现效果、悬浮组件提示效果等。

### 5.4 CSS Transform

- 需要使用至少 1 处 CSS `transform`。
- 可用于卡片悬停放大、按钮缩放、位移动画等效果。

## 6. 页面表现要求

- 页面风格按移动端商城首页实现。
- 页面需要具备清晰的模块分区，能看出 header、content、footer 的层次。
- 页面应优先适配手机端视觉布局。
- 即使在浏览器中预览，也应体现移动端界面风格。

## 7. 非必须实现的内容

- 不要求动态交互逻辑。
- 不要求使用 JavaScript 实现功能。
- 不要求接入后端、接口或数据联动。

## 8. 开发时可直接对照的最小完成清单

- 完成 `src/index.html` 页面结构。
- 完成 `src/style.css` 页面样式。
- 页面包含搜索框、商品分类、Banner、商品列表、底部导航栏。
- 页面包含 `header`、`content`、`footer` 基本结构。
- 页面包含 1 个悬浮组件。
- 页面使用至少 5 个 Flex 相关属性。
- 页面使用至少 2 种伪类 / 伪元素。
- 页面实现至少 1 处 CSS 动画。
- 页面实现至少 1 处 CSS `transform`。
- 所有图片放在 `src/assets/`。
- 不写内联样式，不在 HTML 中写 `<style>`。
