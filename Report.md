# 第一次作业报告

**姓名：** 客昱阳 
**学号：** 23301037 
**作业名称：** 爱风淘——移动端商城静态页面

---

爱风淘（LetsKafu），一个二次元周边/数码物品交易平台

作业使用 **纯HTML、CSS** 开发，并已完成**所有基础与附加功能**


效果网页一键访问：[爱风淘](https://kyy008.github.io/LetsKafu/src/)
## 1. 页面结构

页面整体采用了模拟手机屏幕的外框容器嵌套结构，内部采用语义化标签分为三个主要部分：

- **header顶部区域**：包含系统状态栏模拟时间与电量、带有搜索框和登录按钮的行，以及支持横向滑动的商品分类导航。
- **content主体内容**：包含带有无限循环动画的广告横幅 Banner 和瀑布流式双列商品列表。
- **footer底部导航**：固定在页面底部的功能导航栏，包含首页自定义图标、消息、购物车和我的爱风淘四个入口。
- **附加组件**：固定定位在页面右下角的回到顶部悬浮按钮。

## 2. 页面组件说明

| 组件 | 位置 | 说明 |
|------|------|------|
| 状态栏 | header 最顶部 | 模拟手机系统时间、5G信号和电量显示 |
| 搜索行 | header 顶部 | 包含带相机图标的搜索框居左和独立的登录按钮居右 |
| 商品分类 | header 底部 | 包含 7 个分类项，支持水平滚动和隐藏滚动条，悬浮有旋转动画 |
| 广告横幅 | content 顶部 | 全宽 Banner 图，带有自动无缝循环轮播动画和底部小圆点指示器 |
| 商品列表 | content 主体 | 双列流式卡片布局，卡片包含图片、两行截断的标题、不同字号拼接的价格和销量，悬浮有放大和发光特效 |
| 底部导航栏 | footer | 包含首页图片图标及其他三个 SVG 图标选项，悬浮时有摇摆动画，始终固定在页面底部 |
| 悬浮按钮 | 页面右下角 | 绝对定位的圆形按钮，包含向上箭头 SVG，点击可平滑滚动回到顶部 |

## 3. Flex 布局使用

在 `style.css` 中共使用了以下 flex 属性：

| 属性 | 使用位置 | 作用 |
|------|----------|------|
| `display: flex` | header、footer、商品信息区等 | 开启 flex 容器，实现一维自适应布局 |
| `flex-direction` | phone-screen、content、product-column 等 | 改变主轴方向为 column，实现垂直排列布局 |
| `justify-content` | footer、status-bar、search-row 等 | 控制子元素在主轴上的对齐方式，实现两端对齐或居中对齐 |
| `align-items` | header、nav-item、search-bar 等 | 控制子元素在交叉轴上的对齐方式，实现垂直居中或顶部对齐 |
| `flex-wrap` | product-meta | 控制子元素超出容器宽度时强制不换行 |
| `gap` | category-nav、product-column、search-bar 等 | 简洁地设置 Flex 子项之间的固定间距，替代 margin |
| `flex-shrink` | content、nav-item、footer 等 | 控制子元素的放大比例、缩小比例或占据剩余空间的综合能力 |

## 4. 悬浮组件

页面右下角设置了一个**返回顶部悬浮按钮**，为了在模拟手机屏幕的容器内准确定位，使用了 `position: absolute` 相对于手机屏幕容器进行绝对定位，并配合 `z-index` 确保其始终显示在内容上方右下角。

> 对应代码位置：`src/style.css` 第 741 行

## 5. 伪类 / 伪元素

使用了以下几种伪类和伪元素：

1. **`:hover` / `:focus-visible`**：应用于交互元素，如商品卡片鼠标悬停时触发放大、发光阴影和取消滤镜；底部导航项悬停时触发图标颜色变化和摇摆动画；分类徽章悬停时触发外发光和图标旋转。同时使用 `:focus-visible` 保证了键盘无障碍访问时的视觉反馈。
2. **`::before`**：用于底部导航栏非首页图标的背景效果。通过绝对定位创建一个圆形背景，在默认状态下透明并缩小，当鼠标悬停时通过调整透明度和缩放比例，实现从图标中心向外展开的背景高亮动画。
3. **`:has()`**：在商品网格布局中，当鼠标悬停在某个商品卡片上时，通过 `.product-grid:has(.product-card:hover)` 选中整个网格，将其余未被悬停的商品卡片饱和度降低，并将同列下方卡片下移、异列卡片缩小避让，实现高级的连带交互特效。
4. **`:first-child` / `:last-child`**：用于广告横幅下方的指示器小圆点，分别为第一个和最后一个圆点绑定不同阶段的颜色闪烁动画，模拟轮播图当前进度的视觉效果。
5. **`:not()`**：在底部导航栏中，通过 `.nav-item:not(.nav-item--home)` 排除掉首页这一特殊样式项，仅为其余普通导航项应用悬停时的背景展开和摇摆动画。

## 6. 附加项加分

大量使用高级的 CSS 动画和变形效果，提升了页面的视觉层次和交互体验。具体应用情况如下：

### 6.1 CSS 动画（@keyframes）

共使用了 5 个自定义动画，详情如下：

| 动画名称 | 使用位置 | 作用 |
|----------|----------|------|
| `banner-carousel` | line315:广告横幅图片容器 | 实现 Banner 图片的自动、无缝、平滑左移轮播效果 |
| `banner-dot-first` | line356:广告横幅第一个指示器圆点 | 配合 `step-end` 属性，实现第一个圆点在轮播对应阶段的高亮闪烁 |
| `banner-dot-second` | line369:广告横幅第二个指示器圆点 | 配合 `step-end` 属性，实现第二个圆点在轮播对应阶段的高亮闪烁 |
| `category-icon-spin` | line207:商品分类图标 | 在鼠标悬停或获取焦点时，实现图标顺时针 360 度平滑旋转 |
| `nav-icon-wiggle` | line625:底部导航栏（非首页）图标 | 在鼠标悬停时，实现图标多段式、带角度的左右摇摆动画反馈 |

### 6.2 CSS Transform 变形

全篇共使用了 21 处 `transform` 属性，主要应用如下：

| Transform 函数 | 使用位置 | 作用 |
|----------------|----------|------|
| `translateX` | line79, line337:顶部扬声器、广告横幅圆点容器 | 配合绝对定位 `left: 50%` 使用，实现元素的绝对水平居中对齐 |
| `translateX` | line318-327:广告横幅图片容器 | 在 `@keyframes` 中动态改变 X 轴位移，实现图片的轮播滚动 |
| `translateX` | line470, line475:商品卡片交互 | 配合 `:has()` 伪类，在某卡片被悬停时，使异列卡片在 X 轴方向产生轻微位移避让 |
| `translateY` | line773:悬浮按钮 | 鼠标悬停时实现 Y 轴向上轻微移动，模拟按钮上浮反馈 |
| `translateY` | line480:商品卡片交互 | 配合 `:has()` 伪类，在某卡片被悬停时，使同列下方卡片在 Y 轴方向大幅下移避让 |
| `scale` | line484:商品卡片 | 鼠标悬停时整体放大至 1.1 倍，突出显示当前卡片 |
| `scale` | line470, line475:商品卡片交互 | 配合 `:has()` 伪类，使未被悬停的周边卡片缩小至 0.9 倍，增强主次对比 |
| `scale` | line670, line727:底部导航栏背景 | 实现悬停时圆形背景从 0.8 倍缩放至 1 倍的展开效果 |
| `rotate` | line209-213, line627-647:商品分类图标、底部导航图标 | 在 `@keyframes` 动画中动态改变旋转角度，实现旋转和摇摆视觉效果 |

## 7. 素材来源

- 商品卡片图片素材：截图自京东 H5 页面 / 淘宝 H5 页面/ 闲鱼 H5 页面
- banner 图片素材：Pixiv / Bilibili / 闲鱼H5页面
