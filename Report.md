# 第一次作业报告

**姓名：** ___  
**学号：** ___  
**作业名称：** 移动端商城静态页面

---

## 1. 页面结构

页面整体分为三部分：

- **header**：包含搜索框和商品分类导航
- **content**：包含广告横幅（Banner）和商品列表
- **footer**：底部导航栏，含首页、分类、购物车、我的等入口

## 2. 页面组件说明

| 组件 | 位置 | 说明 |
|------|------|------|
| 搜索框 | header 顶部 | 带放大镜图标的输入框 |
| 商品分类 | header 底部 | 横向滚动的分类标签 |
| 广告横幅 | content 顶部 | 全宽 Banner 图 |
| 商品列表 | content 主体 | 双列卡片布局展示商品 |
| 底部导航栏 | footer | 固定在页面底部 |

## 3. Flex 布局使用

在 `style.css` 中共使用了以下 flex 属性：

| 属性 | 使用位置 | 作用 |
|------|----------|------|
| `display: flex` | header、商品卡片等 | 开启 flex 容器 |
| `flex-direction` | ___ | ___ |
| `justify-content` | ___ | ___ |
| `align-items` | ___ | ___ |
| `flex-wrap` | ___ | ___ |
| `gap` | ___ | ___ |

## 4. 悬浮组件

页面右下角设置了一个**悬浮客服按钮**（或返回顶部按钮），使用 `position: fixed` 实现，始终显示在视口右下角。

> 对应代码位置：`src/style.css` 第 ___ 行

## 5. 伪类 / 伪元素

使用了以下 2 种：

1. **`:hover`**：商品卡片鼠标悬停时添加阴影效果，提升交互反馈
2. **`::after`**（或 `::before` / `:nth-child` 等）：___ （说明用途）

## 6. 附加项（加分）

- [ ] **CSS 动画**：___ （例：Banner 图使用 `@keyframes` 实现淡入效果）
- [ ] **CSS transform**：___ （例：商品卡片 hover 时使用 `transform: scale(1.02)` 放大）

## 7. 素材来源

页面图片素材来源：___（例：截图自京东 H5 页面 / 自行绘制 / 使用占位图）
